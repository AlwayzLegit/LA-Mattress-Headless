# Deep Audit Report — mattressstoreslosangeles.com (2026-07-02)

## Executive summary

Overall posture: this is an unusually well-engineered headless storefront — canonicals, robots, heading hygiene, HMAC-verified webhooks, timing-safe auth, a parser-based sanitizer, and zero unnamed buttons all checked out clean — but the audit surfaced 10 P1s (0 P0s) clustered in three themes: broken buy-flows, a stalled data pipeline, and privacy/perf debt in the analytics stack. The five things that matter most:

1. **Tablet purchase blocker (ux-pdp-01):** on 881–1100px viewports the sticky Add-to-cart opens an invisible variant sheet and locks scroll — a hard conversion dead zone on every multi-size mattress PDP. One-line CSS fix.
2. **Frozen SEO pipeline (seo-tech-01):** the daily URL-inventory refresh has failed 14 days straight, freezing sitemap, edge redirects, and blog archives at the 2026-06-18 snapshot — and its failure alert reaches no one.
3. **PII in session replay (secpriv-01):** PostHog replay records unmasked name/email/ZIP typed into the CCPA privacy-request form itself, plus newsletter emails, for ~10% of sessions.
4. **Large, cheap organic upside:** 119 striking-distance keywords sit at pos 4–15 on six existing collections (seo-organic-01), while ~30% of PDP titles render with a broken mid-suffix ellipsis in SERPs (seo-tech-02) and a 123k-volume blog cluster is self-cannibalized to pos 41–48 (seo-organic-02).
5. **PDP performance self-sabotage (perf-img-01 + analytics stack):** every PDP preloads the full-resolution original product JPEG for a 48px thumbnail, and five analytics stacks (63KB-gzip PostHog statically bundled, gtag preloaded against LCP, recorder started then discarded for 90% of sessions) tax every page load.

Counts: **P0: 0 · P1: 10 · P2: 30 · P3: 26** (70 finding ids in 66 rows after 3 cross-area merges) · **killed: 1**.

---

## Priority register

Severity rules applied: where both verifiers agreed on an adjustment it is applied (noted "adj."); where only one verifier proposed a change, the original severity is kept and the disagreement is noted ("split"). Killed findings are excluded from tiers (see appendix).

### P0

None.

### P1

| id | area | finding | evidence | impact | effort | proposed fix |
|---|---|---|---|---|---|---|
| ux-pdp-01 | design-ux | Tablet buy-flow dead zone (881–1100px): sticky ATC bar opens an invisible variant sheet and locks page scroll | globals.css:2845 shows bar at ≤1100px; globals.css:2891-2893 hides sheet/scrim at ≥881px; buy-box.tsx:345 opens sheet; sticky-variant-sheet.tsx:64 body-scroll-locks unconditionally; no matchMedia guard exists | Hard add-to-cart blocker on the highest-intent template for iPad Pro portrait, Android tablets, half-snapped laptops: frozen unscrollable page, no visible dialog, Esc-only escape | S | Align breakpoints (bar ≤880px OR sheet hidden only ≥1101px), fix stale comment at globals.css:2848-2851, add matchMedia guard in BuyBox |
| ux-css-02 | design-ux | `--container-pad` token used 5x but never defined — padding collapses to 0 on PLP content block (77 collections), sibling nav, mobile announcement bar, HTML sitemap, dashboard *(split: one verifier P2 — parent `.container` padding partially mitigates PLP)* | Zero definitions repo-wide; usages at globals.css:336, 3851, 3904, 5814, 5856; undefined var() → declaration invalid-at-computed-value-time → padding 0 | Edge-to-edge text on primary browse surfaces for the majority-mobile audience; announcement bar copy touches viewport edges on phones | S | Define `--container-pad: var(--gutter);` in :root (or replace the 5 usages), verify PLP/announcement rhythm |
| secpriv-01 | security | PostHog session replay records unmasked PII from CCPA and newsletter forms | analytics-posthog.tsx:60-66 `maskAllInputs:false` (overriding PostHog's mask-by-default), only `[data-ph-mask]` + password masked; CCPA inputs (data-opt-out-form.tsx:131/135/141) and newsletter email carry no mask attribute; zero form usages of data-ph-mask found | ~10% of sessions ship the exact PII a visitor submits to keep private (name/email/ZIP + free-text note) into third-party replay storage — disclosure/retention liability undermining the CCPA workflow | S | Set `maskAllInputs:true` (unmask non-PII fields explicitly) or add data-ph-mask/data-ph-no-capture to all PII inputs; prefer mask-by-default |
| seo-tech-01 | seo-technical | Daily URL-inventory refresh workflow has failed 14 days straight — sitemap, redirect table, blog archives frozen at 2026-06-18 snapshot *(split: one verifier P2 — failures verified live and chronic, incl. pre-June flakiness)* | GitHub Actions: last success 2026-06-19T09:03Z, then 14 consecutive scheduled failures through 2026-07-03; last snapshot commit cbc8d18; app/sitemap.ts + lib/redirects-table.ts both build from the frozen snapshot; failure-notification step (refresh-inventory.yml:170) reaches no one | Everything the sitemap advertises is 15 days stale; new products/redirects since 6/18 missing from sitemap and edge redirects | M | Pull failing-run logs (likely expired Shopify Admin token or codegen break ~6/19), fix root cause, workflow_dispatch a fresh snapshot, route failure alerts somewhere read (or auto-file an issue) |
| seo-tech-02 | seo-technical | 66 of 218 PDP `<title>`s render with a mid-string ellipsis — 25 cut inside the brand suffix ('· LA Mattres…') — because capTitle's brand-suffix regex doesn't know '·' | Live capture pdp-standard-foundation-box-spring.html shows broken title/og:title; lib/seo.ts:34 BRAND_SUFFIX_RE matches only `[|–—-]`; PDP page.tsx appends '· LA Mattress Store'; code-path simulation over 218 products: 66 ellipsised, 25 mid-suffix | ~30% of catalog shows visibly broken titles in SERPs and on every social share — the exact failure mode Phase 292 was built to prevent; CTR cost on top commercial pages | S | Add '·' to BRAND_SUFFIX_RE, or have the PDP guard reserve suffix room via ensureTitleDistinctFromH1; backfill distinct seo.title for the 64 seoTitle==title products (see shopify-hyg-04) |
| perf-img-01 | performance | PDP downloads the full-resolution ORIGINAL product image (untransformed, preloaded) for a 48px sticky-bar thumbnail | pdp-sticky-atc-bar.tsx:58-64 raw `<img src={productImage.url}>`; capture head contains BOTH the optimized srcset preload AND a second raw `<link rel="preload">` of the untransformed original | Every PDP visitor double-downloads the hero photo, with the raw multi-hundred-KB fetch competing at preload priority against the actual LCP image on all 218 products | S | Add Shopify CDN transform params (`width=96&format=webp&quality=75`) or use next/image sizes="48px"; re-capture PDP head to confirm the raw preload disappears |
| seo-organic-01 | seo-organic | 119 striking-distance keywords (pos 4–15, vol≥200); mid-pack of 187 kws at pos 4–10 is one CTR tier from top-3 | domain_organic_live_20260703.txt (fresh pull, all numbers independently reproduced): helix 90.5k/pos8, helix mattress 60.5k/pos13, bed frame 33.1k/pos15, mattress toppers 27.1k/pos13, queen mattress sale 14.8k/pos10, etc. — all mapped to existing collections | Most of the realistic near-term non-brand upside; helix terms alone are 151k monthly searches on a collection already earning 6.4% of traffic; pos 4–10 → top-3 typically 2–3x CTR | M | On-page + internal-link sprints on the 6 target collections: title/H1 alignment to exact phrasing, FAQ/buying-guide modules, links from high-authority location pages and top posts |
| seo-organic-02 | seo-organic | Blog cannibalization: three posts split the 'full size bed dimensions' cluster (74k + 27.1k + 22.2k vol), all stuck pos 41–48 *(split: one verifier P2)* | 'full size bed dimensions' 74k ranks pos 41 AND pos 47 via two different blog URLs (classic cannibalization); third post takes the 22.2k variant at pos 48; all three in blogs.json | A 123k+/mo-search cluster yields ~0.26% of traffic because three thin pages divide relevance; one consolidated page at pos 5–10 would be a top organic asset | S | Merge into one canonical guide (keep strongest URL, 301 the other two via Shopify redirects), fold in unique content, link from the full-size collection |
| seo-organic-03 | seo-organic | Dual-host indexation: 63/500 keywords (4.3% of traffic) rank under non-www; ~30 paths rank under BOTH www and apex *(split: one verifier P2)* | Live export: www 437 kws/71.2% vs apex 63/4.3%; homepage + money pages (studio-city page ranks on both hosts); canonicals all www but no host-level 301 in middleware.ts/next.config.mjs; Vercel has both hosts attached to the project | Link equity and click signals split across two hosts on money pages (studio-city appears twice in top-15 traffic URLs) | S | Verify/configure apex→www 308 at the Vercel domain level (or add to middleware); could not verify live (curl 403 via audit proxy) — check first when access returns |
| secpriv-02 | security | CSP relies on 'unsafe-inline' + 'unsafe-eval' while rendering a large merchant-HTML injection surface *(split: one verifier P2 — sanitize-html allowlist is a solid primary defense)* | next.config.mjs:107 script-src with both unsafe directives (documented rationale: inline JSON-LD/GA4/Judge.me bootstraps, Judge.me legacy eval); ~30 dangerouslySetInnerHTML render sites | 'unsafe-inline' negates CSP's XSS backstop: lib/sanitize.ts is the ONLY line of defense against stored XSS from Shopify CMS content | L | Nonce-based CSP via middleware (stamp own inline scripts + JSON-LD), sandbox/replace Judge.me legacy widget to drop 'unsafe-eval'; report-only rollout first via existing Sentry report-uri |

### P2

| id | area | finding | evidence | impact | effort | proposed fix |
|---|---|---|---|---|---|---|
| seo-organic-04 | seo-organic | /pages/mattress-store-playa-del-rey ranks #6 for 'mattress store marina del rey' but is absent from the Shopify pages inventory with no redirect *(adj. P1→P2, both verifiers)* | Live export row (pos 6, /pages/mattress-store-playa-del-rey); handle absent from pages.json (147 handles); redirects cover only the different /pages/mattresses-in-playa-del-rey | A ranking local page deleted without a redirect is or will be 404ing to searchers, losing a top-10 coastal-westside ranking | S | Add Shopify redirect → /pages/mattress-store-marina-del-rey (exists, published) or recreate the page; verify live HTTP status first (bot-block prevented check) |
| seo-tech-04 + ux-404-07 + a11y-404-03 | seo-tech / design-ux / a11y | **[MERGED — one root cause]** Live 404s serve an empty `__next_error__` shell: no lang attribute, zero links/headings/landmarks in server HTML; the branded not-found UI exists only in the RSC flight payload *(split on seo-tech-04: one verifier P3; a11y & UX verifiers held P2)* | 404-page.html capture: `<html id="__next_error__">` (no lang), body = empty hidden div + scripts; 0 `<a>`, 0 h1–h6; recovery UI (app/not-found.tsx:38-89) only in `self.__next_f` payload; chunk manifest implicates notFound() from pages/[handle] | With 2,029 legacy redirects, dead-URL traffic is common: no-JS/slow-JS users get a blank white page (2–5s on mid-mobile), SR users get a language-less empty document (WCAG 3.1.1), raw-HTML crawlers see nothing | M | Diagnose the CSR bailout (dynamic API without Suspense boundary, or notFound() after shell flush); confirm `next build` prerenders /_not-found; acceptance: curl of a dead URL returns HTML with `lang="en"` and the branded h1 |
| seo-organic-05 | seo-organic | 26 published 'mattress-store-<neighborhood>' pages are thin (0–1,500 chars, zero page metafields) and earn zero top-500 rankings | 26 published handles in pages.json; body lengths 0–1,379 chars (west-hollywood/westwood literally empty); 0 page-type metafield rows; none of the 26 URLs in the live top-500 | Programmatic local cluster producing nothing: crawl waste, doorway-pattern risk, unrealized demand the template demonstrably wins elsewhere (real-showroom pages pull 5.7–7.9% each) | M | Rebuild the 8–10 highest-demand neighborhoods with unique content modeled on winning showroom pages; prune/consolidate the rest into /pages/mattress-store-locations |
| seo-organic-06 | seo-organic | Traffic concentration: 6 URLs = 47.8% of organic traffic; 45% of all traffic is local/near-me intent on 5 pages *(split: one verifier P3 — expected shape for a brick-and-mortar local retailer)* | Recomputed exactly from live export: / 13.2%, west-la 7.9%, studio-city 7.4%, koreatown 7.1%, helix 6.4%, glendale 5.9%; 86 local-intent kws = 45% of traffic | Portfolio risk: an algorithm shift on local intent or one lost location ranking moves double-digit traffic; non-brand diversification gap (collections 20%, products 0.06%) | L | Treat as portfolio directive steering findings 01/02/07: grow non-brand collection + guide clusters until no page class exceeds ~30% |
| seo-organic-07 | seo-organic | Intent gap: blogs are 52% of ranking keywords but 11.6% of traffic; PDPs invisible (2 kws, 0.06%); #1 blog asset reviews a competitor retailer (Sam's Club, 2.87% of site traffic) | Section split from live export: blogs 258 kws/11.6%, collections 158/20.0%, pages 42/30.5%, products 2/0.06%; Sam's Club post is 8th-biggest traffic URL | Largest content asset attracts shoppers seeking a competitor's product with no conversion path; 218 products earn ~zero organic entries | M | Add compare/conversion modules to the warehouse-club review posts; target long-tail model-name queries on the top-20 revenue PDPs (schema + unique copy) |
| seo-organic-08 | seo-organic | La Brea/Hancock Park showroom (5th store) has zero organic visibility while the other four each pull 5.7–7.9% of traffic | best-mattress-store-la-brea published but 0 keywords/rankings in top 500; other four showrooms pos 1 on their 'la mattress stores <area>' terms; santa-monica page split (published thin dupe vs unpublished full page) | One of five physical showrooms contributes nothing organically; Mid-Wilshire 'near me' demand lost despite a proven template | S | Bring La Brea page to parity (content depth, LocalBusiness schema, locations/homepage links, GBP alignment) |
| seo-tech-03 | seo-technical | 72 live beds-mattresses articles + the live blog index are indexable and internally linked but excluded from sitemap.xml — sitemap policy contradicts the /blogs hub policy | sitemap.ts:65 DEPRECATED_BLOG_HANDLES on a now-false comment ('every URL 301s'); blogs hub page deliberately re-included the blog; recomputed: 184 published, 112 redirected, 72 live at 200, none in sitemap; automation still publishes INTO the deprecated blog | 72 indexable URLs carry no sitemap discovery signal; sitemap disagrees with the site's own link graph; automation keeps generating orphans needing manual redirects | S | Pick one policy: un-deprecate (existing filters emit only the 72 live URLs) or finish deprecation (301 the 72, re-exclude from hub); repoint the daily-blog automation; fix the stale comment + thin Blog JSON-LD |
| shopify-hyg-01 | shopify-hygiene | 9 collections have no seoTitle AND no seoDescription, including twin-xl-mattress-sale (126 products) | collections.json: 9 named handles with both fields null and empty description; app falls back to templated copy (collections/[handle]/page.tsx:63-71) | Six sizable PLPs serve generic templated meta instead of keyword-tuned copy; the 9 were simply missed in the Phase-2 migration | S | Write seoTitle/seoDescription in Shopify Admin for the 8 non-empty collections, matching the tuned-collection pattern |
| shopify-hyg-02 | shopify-hygiene | 6 published, internally linked collections have only 1–3 products (thin PLPs), some carrying 14KB of SEO content over a near-empty grid | collections.json exact counts (mattress-toppers=1 … technogel=3); harvest-mattresses linked site-wide (all 6 captures); organic-mattress has 14,622-char seo_content over 3 products | Crawlable sitemap-included PLPs reading as doorway-ish; convert poorly; harvest linked from every page with 2 products behind it | M | Merchandise to 6+ products or consolidate: redirect comforters/protector into bedding, fold technogel/harvest into brand pages |
| shopify-hyg-03 | shopify-hygiene | 4 floor-sample clearance products use metafield key custom.sleep_position (singular); PLP filters query custom.sleep_positions (plural) *(split: one verifier P3 — already known/tracked; impact claim rests on a curated 81-row CSV)* | Audit CSV rows 78-81 carry the singular key; filters.ts:73/143 query only the plural key | The 4 clearance mattresses (high-margin floor samples) never match the Sleep Position filter | S | Re-key the 4 values to custom.sleep_positions in Shopify Admin; delete the orphan singular entries |
| codeq-env-example-01 | code-quality | 11 env vars used in code are missing from .env.example — including the two that gate /admin and the chat API | Diff of all process.env refs vs .env.example: ADMIN_USER/ADMIN_PASSWORD (middleware 503s /admin without them), ANTHROPIC_API_KEY, CHAT_USE_HOSTED_MCP, SALE_PAGE_PREVIEW_TOKEN, PostHog server+client vars, etc.; plus stale SHOPIFY_ADMIN_TOKEN comment | Fresh clone from .env.example ships with dead admin, dead chat, no analytics — with no hint why | S | Add the 11 vars with one-line comments; correct the SHOPIFY_ADMIN_TOKEN comment |
| codeq-test-gaps-02 | code-quality | Zero test coverage on sitemap.ts, lib/seo.ts, and middleware composition — the three riskiest untested surfaces | 57 test files, none import these; capTitle already caused one real incident (41 dup titles, documented in-file); sitemap redirect-exclusion prevents a documented Semrush failure; middleware precedence of 4 redirect passes untested | Regressions here cost rankings (sitemap/titles) or break /admin + 2,000+ redirects; all near-pure and cheap to pin | M | Three test files in the existing tests/ssr pattern: sitemap exclusions + www origin; capTitle/ensureTitleDistinctFromH1 table test; middleware NextRequest fixtures (503/401/redirect precedence) |
| codeq-phone-literals-03 | code-quality | Canonical phone bypassed by hardcoded literals; chat surfaces present the Koreatown showroom line as the 'general line' | Canonical (800) 218-3578 in lib/site-config.ts (sanitize.ts even rewrites stale numbers for NAP); 7 hardcoded 800 literals in service-pages/faq-extra; chat error copy + system prompt + plp-content.ts:255 use (213) 984-4654 — the Koreatown line per showrooms.ts:62 | Phone change in the metaobject leaves 13+ stale literals; chat outages route shoppers to one showroom instead of the tracked 800 line, contradicting enforced NAP consistency | S | Import SITE_PHONE_* constants in the data files; for chat, either single CHAT_FALLBACK_PHONE constant (if 213 intentional) or switch to the 800 line |
| codeq-god-admin-page-04 | code-quality | God file: app/admin/page.tsx is 2,792 lines — one 1,980-line server component + ~20 inline chart components *(split: one verifier P3 — internal-only surface)* | wc -l = 2792; DashboardPage spans lines 100–2081; ~20 presentational components at 2082–2792; extraction seam already exists at app/admin/_components/ | Any dashboard change churns a 2.8k-line file; monolith serializes all section fetches instead of streaming | L | (1) Cut-paste the 20 components into _components/ (~700 lines); (2) split into per-section async server components in `<Suspense>` to parallelize Admin-API calls |
| codeq-npm-audit-07 + secpriv-09 | code-quality / security | **[MERGED]** 2 high-severity transitive vulns — undici via cheerio (TLS-validation bypass, Set-Cookie injection) and protobufjs via posthog-js/otel (DoS) — all with fixes available | npm-audit.json: 2 high, 18 moderate, 1 low, 0 critical, all fixAvailable=true; chains verified via npm ls (cheerio@1.2.0→undici@7.25.0; posthog-js→otlp-transformer→protobufjs@7.6.0) | Practical exploitability low today (parsing/telemetry positions), but 'high + fixAvailable' is what scanners and merchant security questionnaires flag; fix nearly free | S | `npm audit fix`, re-run tsc/lint/ssr tests; add monthly `npm audit --audit-level=high` CI step |
| perf-3p-02 | performance | PostHog session-recorder bundle downloaded/started for ~100% of sessions, then discarded for 90% *(adj. P1→P2; verifiers split P2/P3 — recorder load is also gated on remote config, softening the original mechanism)* | analytics-posthog.tsx:66 `disable_session_recording:false`, then post-init `Math.random() > 0.1 → stopSessionRecording()`; recorder.js measured 98,586 B raw / 30,447 B gzip | ~30KB gzip + rrweb DOM-observer startup CPU wasted during hydration for 9 of 10 sessions (subject to remote-config race) | S | Decide sample BEFORE init (`disable_session_recording: !sampledIn`) or set replay sample rate server-side in PostHog; persist decision in sessionStorage |
| perf-js-03 | performance | posthog-js (63KB gzip) statically imported into the shared client bundle of every page — the code comment claiming lazy import is false | Static imports at analytics-posthog.tsx:5, analytics-ga4.tsx:6, lib/analytics.ts:30; no dynamic import repo-wide; module.js measured 195,204 B raw / 63,406 B gzip; stale 'lazy-imported' comment at :29-30 | Largest identifiable third-party chunk in First Load JS on every route, delaying hydration/TTI on mobile | M | Convert to `await import('posthog-js')` inside init/first-track with a pre-init queue; fix comment; verify with next build first-load diff |
| perf-3p-04 | performance | Five analytics stacks ship on every storefront page; Core Web Vitals reported to three of them | layout.tsx:153-168 mounts Vercel Analytics, Speed Insights, GA4, Shopify beacon, PostHog; analytics-ga4.tsx:44-74 triple-forwards every CWV metric | Cumulative script weight, five beacon chains per pageview, duplicated CWV collection — the biggest recurring third-party tax and standing INP/TBT cost on mid-range mobile | M | Pick one CWV sink; evaluate dropping @vercel/analytics (pageviews covered by GA4+PostHog); keep Shopify beacon + GA4 per documented purposes |
| perf-3p-05 | performance | GA4 gtag.js is preloaded in `<head>` on every page, contradicting the documented 'doesn't compete with LCP' intent | All content captures contain the gtag preload; emitted by next/script afterInteractive (analytics-ga4.tsx:80-83) whose own comment (:15-16) says it was chosen to avoid LCP contention | Preload scanner starts the ~130KB gtag transfer in parallel with the LCP hero on bandwidth-constrained mobile; analytics gains nothing from arriving early | S | Switch to strategy="lazyOnload" (no head preload), or inject manually after window load if early-bounce capture matters |
| perf-css-06 | performance | Single 283KB (62KB gzip) render-blocking globals.css on every route, including ~21KB admin-only and ~10KB provably dead rules | Measured 283,446 B raw / 62,126 B gzip, imported once in app/layout.tsx:3; byte census: pdp 27KB, dash/admin ~21KB, plp 18KB…; 75 of 1,216 class tokens unreferenced anywhere (10,308 B dead) | Every visitor's critical path carries admin/chat/quiz/compare/retired-review CSS; 62KB gzip is 2–4x a commerce budget and grows monotonically | M | Delete the 75 dead classes (CI-gate the census script); move dash-/acct- rules into an admin-layout-only CSS file; longer-term split per-surface families |
| perf-isr-07 | performance | All 77 collection PLPs are force-dynamic — top commercial landing pages pay full SSR TTFB on every request | collections/[handle]/page.tsx:41 `force-dynamic` (searchParams rationale) overrides generateStaticParams; every other key template is static/ISR | Primary SEO entry points get server-render latency on every hit instead of edge-cached HTML, inflating TTFB→LCP and crawl cost, though filtered/sorted variants are a minority of hits | L | Serve bare PLP URLs from ISR (revalidate + webhook tag busting like PDPs); keep dynamic only when searchParams present (PPR or client boundary) |
| perf-img-08 | performance | Homepage gives fetchPriority=high + head preloads to 3 below-the-fold product-card images, competing with the hero LCP | popular-products.tsx:55 `priority={i < 3}`; capture shows 4 imagesrcset preloads (hero + 3 cards), section starts ~880px down per the page's own comment | Three below-fold images fetched at the same priority as the hero LCP — the exact contention the hero's quality=50 tuning was added to win | S | priority={false} for homepage PopularProducts cards (keep on PLPs where first cards are genuine LCP candidates) |
| ux-plp-03 | design-ux | Financing math contradicts itself: same $2,299 mattress shows 'From $96/mo' on the PLP card (÷24, Synchrony) and 'From $191.58/mo with Affirm' on the PDP (÷12) | financing-calc.ts:16 months=24 for cards; pdp-financing-line.tsx:42 `amount / 12`; live captures confirm both figures; card comment claims it 'mirrors the PDP callout' — the opposite is true | Monthly price doubles at the browse→decision click on $1,500+ mattresses where financing is the stated ATC-drop remedy; reads as bait-and-switch | S | One canonical monthly framing from a single shared helper feeding both PlpCard and PdpFinancingLine, with provider labeled on both |
| ux-mobile-04 | design-ux | Compare tray floats on top of the mobile sticky ATC bar on PDPs (z-80 over z-60, both bottom-anchored); chat bubble was lifted for this exact collision, the tray was not | globals.css:432-438 tray fixed bottom:16px z-80; :2835-2846 sticky bar z-60 ≤1100px; tray renders on /products/ (compare-store.ts:60); chat got the lift (:599-601), no equivalent tray rule exists | Shoppers who compared on the PLP get the compare pill over the sticky bar's title/price and, on narrow phones, part of Add-to-cart — the highest-intent segment | S | Mirror the chat fix: `body.pdp-atc-visible .compare-tray { bottom: calc(85px + env(safe-area-inset-bottom)); }` (body class already maintained) |
| ux-mobile-05 | design-ux | Chat FAB (z-90) floats over the PLP filter bottom-sheet (z-60), covering the right end of the full-width 'Show results' button on phones | globals.css:571-574 bubble z-90 vs :4302-4311 sheet z-60; no chat-hide body class for the filter sheet; team already fixed the same class of bug for the PDP sheet (:602-604) and cart drawer (z-200) | Mis-taps open chat mid-filtering; the primary apply action is partially obstructed on the template where filtering IS the shopping activity | S | Toggle a body class from filter-sheet open state + `body.plp-filters-open .chat-bubble { opacity:0; pointer-events:none; }`, or raise the sheet above z-90 |
| ux-plp-06 | design-ux | Compare toggle pops in only after hydration on every PLP card — the 24-card grid reflows ~40px per card under the shopper's thumb | Capture: 0 compare-toggle occurrences across 24 SSR'd cards (compare-toggle.tsx:54 returns null pre-hydration); button is in normal flow, ~40px effective height | Full-grid layout shift at hydration, feeding the PLP CLS/LCP tails the code's own comments cite (PostHog p95 4–6s LCP); mis-taps on slow connections | M | SSR the toggle in unselected default state (reconcile after hydration), or reserve fixed-height placeholder space in the card |
| ux-css-08 | design-ux | Breakpoint sprawl: ~20 distinct media widths with 760 vs 768px near-duplicates, one already-shipped override regression documented in-file, and the referenced design-system doc missing | ~20 max-width values in globals.css; in-file admission at :4281-4284 of a shipped tap-target regression; tokens header points to /design-handoff/02-design-system.md which doesn't exist; concrete seams: 769–1024px tablets get hamburger nav with 40px targets; 881–1024px mixes mobile nav + desktop filters | Every new component picks its own width; regressions of this class have shipped once already and review can't diff against a canonical set; produced the P1 ux-pdp-01 | L | Define 4–5 canonical breakpoints as documented constants, migrate the 760→768 and 860–920→1024 clusters template by template, restore the design-system doc; fold tap-target rule into the hamburger breakpoint first |
| a11y-contrast-01 | a11y | Unavailable variant chips render 13–14px text at 2.05:1 — fails WCAG 1.4.3 and even the 3:1 large-text floor; the chips are NOT disabled controls (so no exemption) | globals.css:23 --text-4 #B5B5B5; :2983/:3005 apply it to unavailable size/firmness chips; buy-box.tsx:229-236 + sticky-variant-sheet.tsx:124 keep them as operable buttons with onClick | Low-vision shoppers can't read which options exist-but-unavailable — exactly what they need before calling a showroom or picking another size; all 218 products, desktop + mobile sheet | S | Change unavailable-chip text to var(--text-3) #6B6B6B (5.33:1) keeping the diagonal strikethrough as the non-color cue; leave genuinely disabled stepper buttons as-is |
| secpriv-05 | security | Admin Basic Auth has no rate limiting or lockout; brute-force resistance rests entirely on ADMIN_PASSWORD entropy *(split: one verifier P3 — /admin does front Shopify orders/PII, but credential is operator-controlled)* | middleware.ts:183-202: timingSafeEqual compare, 401 on mismatch, no attempt counter/backoff; lib/rate-limit.ts not invoked (edge in-memory wouldn't hold anyway) | Unlimited credential guesses at edge speed against a surface exposing orders/customer data; no 2FA, no 401-burst alerting | M | Durable (KV/Upstash) attempt counter with backoff and/or front /admin with platform auth; minimum: Sentry alert on 401 bursts + enforced password length |
| secpriv-06 | security | Newsletter endpoint subscribes arbitrary emails with SINGLE_OPT_IN and no verification *(split: one verifier P3 — 're-subscribes unsubscribed users' claim not fully supported)* | newsletter/route.ts:86-89 sets SUBSCRIBED/SINGLE_OPT_IN for any regex-valid email; duplicate swallowed as success (:99); only guard is the per-instance limiter | List poisoning / consent injection at volume; CAN-SPAM/GDPR consent-provenance exposure for the merchant | M | Switch to CONFIRMED_OPT_IN (double opt-in) and/or add Turnstile-style bot check; don't blind-upsert existing customers |

### P3

| id | area | finding | evidence | impact | effort | proposed fix |
|---|---|---|---|---|---|---|
| seo-organic-09 | seo-organic | California-king queries land on the wrong collection: 'california king size mattress' (2,400 vol) ranks pos 10 via /collections/king-size-mattresses though the cal-king collection exists | Live export rows; cal-king handle confirmed in collections.json | Both pages capped ~pos 10–12 on ~9,700 combined monthly searches | S | Strengthen the cal-king collection (title/H1/intro/dimensions FAQ) + crosslink from the king collection |
| seo-tech-05 | seo-technical | Homepage OpenGraph block is the thinnest on the site: no og:url, og:site_name, og:locale, or og:image dimensions/alt | Homepage capture has exactly 3 og:* metas; page-level override replaces rather than extends root metadata; canonical trailing-slash mismatch vs JSON-LD @id | Most-shared URL renders from an undersized square logo with no declared dimensions; no ranking impact | S | Add url/siteName/1200×630 image with dims+alt to the homepage openGraph object; align trailing slash |
| seo-tech-06 | seo-technical | Meta descriptions truncate mid-word ('…at traditional bed h...') — truncDescription cuts at a character boundary | PDP capture; lib/seo.ts:80-83 slice + '...' with no word-boundary logic (article pipeline already solves this with truncateToSentence) | Chopped word + ellipsis in SERP snippets for any >160-char source description | S | Back up to the last space (or reuse truncateToSentence moved into lib/seo.ts) |
| shopify-hyg-04 | shopify-hygiene | 135/218 products have seoTitle identical to raw title; 41 exceed 62 chars; 39 seoDescriptions exceed 165 chars (mitigated at render, unfixed at source) | products.json counts; worst offenders listed (7-char 'Restara' … 95-char Diamond title); render mitigation confirmed | Shopify-side values feed non-headless surfaces (Shopping feeds, Shop channel); over-long titles lose trailing keywords to capTitle; the 64 seoTitle==title products feed the seo-tech-02 branch | M | Batch-backfill via the existing seo-backfills pipeline: keyword-led titles ≤60 chars, descriptions 140–160 |
| shopify-hyg-05 | shopify-hygiene | 25 collections hold ~26KB of merchant copy in custom.description_ that no storefront code reads; the May-20 merge recommendation was never executed | Metafields CSV: 25 rows; repo analysis file confirms 'NOT read anywhere' and non-duplicate content | Unique merchant copy invisible to users/crawlers; three parallel description fields invite confusion | M | Execute the documented merge into custom.seo_content, then delete the description_ definition |
| shopify-hyg-06 | shopify-hygiene | 11 zero-product collections (9 seasonal placeholders + metal-beds + headboards); metal-beds carries 2,380 chars of seo_content that can never be seen | collections.json productsCount=0 rows; sitemap correctly excludes them but they return 200 with empty grids; memorial-day-sale still populated a month post-event | Stranded SEO investment; empty seasonal URLs soft-thin at 200 for old links | S | Stock or delete metal-beds/headboards (moving copy); post-event routine: empty + 301 finished seasonal collections to /collections/on-sale |
| shopify-hyg-07 | shopify-hygiene | Published page bodies link to /pages/mattress-warranty and /pages/shipping-and-delivery (301-hop only); /pages/low-price-guarantee linked while unpublished in Shopify (served by coded Next page) | pages-bodies.jsonl hrefs; redirect gids verified; coded-pages.ts:18 covers low-price-guarantee | No broken UX, but redirect hops leak equity/latency; draft-vs-coded split invites accidental cleanup | S | Edit 3 page bodies to final URLs; annotate the code-owned draft |
| shopify-hyg-08 | shopify-hygiene | 238 unpublished draft articles across blogs (151 in beds-mattresses); the 'sales' blog dormant since 2024-05-19 with stale 2024 sale titles | blogs.json per-blog counts; sales blog latest publish 2024-05-19 | Admin clutter; stale 2024 sale copy can rank against current seasonal sale pages | S | Bulk-archive the beds-mattresses drafts after a skim; retire/redirect the sales blog |
| codeq-god-admin-lib-05 | code-quality | God file: lib/shopify/admin.ts is 1,762 lines of 18 unrelated dashboard queries in one module | wc -l; four unrelated domains; extraction pattern already established (lib/dashboard/) | Maintenance churn; untestable without dragging server-only | M | Split into admin/{client,orders,catalog,seo,customers}.ts behind an index barrel; keep moving pure aggregation into lib/dashboard/ |
| codeq-god-pages-handle-06 | code-quality | God file: app/(storefront)/pages/[handle]/page.tsx is 1,556 lines — 5 full page templates inlined in the route dispatcher | 5 inline templates (Locations/Showroom/Sale/Neighborhood/Default); 10+ sibling types already extracted to sections/ | Two conventions coexist; sale/showroom edits churn the shared dispatcher | M | Finish the established migration to app/_components/sections/; page.tsx shrinks to ~300-line dispatch |
| codeq-stale-admin-comment-08 | code-quality | Stale 'Admin API is forbidden' doc in lib/shopify/client.ts (and .env.example) contradicts the shipped /admin dashboard | client.ts:5-7 vs the 1,762-line runtime Admin client; same staleness in .env.example:21-25 | New contributors misled by a false hard rule; erodes trust in otherwise-good inline docs | S | Rewrite both comments: Storefront-only on storefront routes, Admin API confined to /admin via lib/shopify/admin.ts |
| codeq-dead-exports-09 | code-quality | Dead exports: getMenu and the entire lib/shopify/queries/handles.ts module (3 functions) have zero call sites | Barrel-only references; sitemap sources handles from lib/inventory instead | ~90 lines of dead full-catalog-paginating query code — an accidental cost trap if 'reused' | S | Delete both modules + barrel lines; fix README mention; tsc confirms |
| codeq-site-const-dup-10 | code-quality | 8 files hardcode `const SITE = 'https://www.mattressstoreslosangeles.com'` instead of importing SITE_URL, bypassing the apex-guard | 8 file:line grep hits vs sitemap.ts/robots.ts doing it right; SITE_URL exists precisely to force apex→www | No live bug, but canonical/JSON-LD would silently diverge from sitemap/robots on any host change — the drift class canonicalizeSiteUrl prevents (relates to seo-organic-03) | S | Mechanical replace with `import { SITE_URL }` in the 8 files |
| codeq-sanitize-style-11 + secpriv-07 | code-quality / security | **[MERGED]** sanitize.ts allows unfiltered style= on every tag (no allowedStyles) — merchant HTML can carry arbitrary CSS (fixed overlays, url() beacons) | lib/sanitize.ts:65 style in the '*' allowlist, no allowedStyles config; execution channels otherwise tight (schemes pinned, iframes host-pinned) | Attacker must already control Shopify Admin, but this is the one unconstrained channel in an otherwise tight sanitizer: UI-redress + request-initiation surface across every article/PDP/PLP body | S–M | Add an allowedStyles map for the small observed vocabulary; reject position:fixed/absolute and url(); extend the XSS test with an overlay case |
| codeq-action-error-leak-12 | code-quality | Cart server actions return raw upstream error messages to the client UI | cart.ts returns err.message at 8 sites; ShopifyApiError embeds upstream bodies; chat already solved this (friendlyChatError) | On a Storefront outage shoppers see backend jargon in cart toasts; minor detail disclosure | S | One error-mapping helper: friendly fallback for API/Abort errors, keep messages only for validation UserErrors, console.error the raw |
| perf-js-09 | performance | Chat conversation UI statically bundled into every page though it only renders after the launcher click | Static import in chat-widget.tsx:9; ~29KB source in the shared bundle | Parse/hydrate cost on every page for a click-gated feature | S | `dynamic(() => import('./chat-conversation'))` rendered once open |
| perf-img-10 | performance | Logo `<Image>` emits a junk 16-entry srcset (same PNG ×16) plus a pointless priority preload on every page | All captures; lib/image-loader.ts returns local assets unchanged for all widths | ~2KB HTML noise/page + a wasted preload slot ahead of real LCP resources | S | Plain `<img>` (or unoptimized next/image), drop priority |
| perf-font-11 | performance | Geist woff2 preload only appears on prerendered routes — dynamic routes (all PLPs, 404) discover the font late | Capture diff: preload on homepage/PDP, absent on PLP/404/redirected-article despite preload:true in root layout | Extended FOUT window on top entry pages; largely mooted if PLPs go ISR (perf-isr-07) | M | Reproduce locally; add explicit font preload in app/layout.tsx head (React dedupes) |
| a11y-aria-02 | a11y | Footer nav landmarks 'Customer Care' and 'Visit Us' have broken accessible names: aria-labelledby IDREF contains a space | footer.tsx:100 builds id from raw title; capture confirms `id="footer-col-customer care"`; space-separated IDREF tokenizes into two unresolvable lookups | 2 of 4 footer landmarks announce as bare 'navigation' on every page | S | Slugify the title when building the id |
| a11y-motion-04 | a11y | Product-rail scroll buttons force smooth scrolling regardless of prefers-reduced-motion | rail-scroll-buttons.tsx:28 unconditional behavior:'smooth'; CSS kill switch can't affect JS scrollBy; hero-controller does it right | The only motion escaping an otherwise-complete reduced-motion system | S | Gate behavior on matchMedia('(prefers-reduced-motion: reduce)') |
| a11y-headings-05 | a11y | Blog archive skips h1→h3 for all 12 featured/recent article cards | Capture heading sequence [1,3,3,…,2]; first h2 appears only after the grid | SR heading-skim users hear a phantom level | S | Visually-hidden h2 before the grid, or promote cards to h2 |
| a11y-nav-06 | a11y | Desktop primary nav is the only unlabeled nav landmark among 6–8 on every page | nav.tsx:367 no aria-label; all 7 sibling navs named; mobile counterpart labeled 'Primary' | The most important nav is the anonymous one in landmark lists | S | `aria-label="Primary"` to match mobile |
| a11y-linktext-07 | a11y | Two different blog articles share identical title/link text 'How to design a bedroom that actually helps you sleep' | Identical text → two distinct interview URLs (Collins/Vieira); only collision across 551 links checked | WCAG 2.4.4: purposes indistinguishable in an SR links list | S | Differentiate titles in Shopify (append interviewee); no code change |
| secpriv-04 | security | No Strict-Transport-Security header; COOP/CORP also absent *(adj. P2→P3, both verifiers — platform layer may partly cover; verify live headers)* | next.config.mjs:125-148 sets CSP/XFO/XCTO/Referrer/Permissions but no HSTS/COOP/CORP anywhere in repo | First-visit downgrade exposure on hostile networks; no preload-list eligibility | S | Add `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload` (confirm subdomains HTTPS first); consider COOP/CORP same-origin |
| secpriv-08 | security | Preview-enable GET puts the preview token in the URL (server logs + browser history) | preview/enable/route.ts:44-53 reads token from query, 302s; token equals static SALE_PAGE_PREVIEW_TOKEN | Long-lived shared secret leaks into log aggregators/history; draftMode access to parked sale pages; no rate limit on verification | S | Prefer POST activation; rotate to high-entropy or short-lived signed token; rate-limit verifyPreviewToken callers |
| secpriv-10 | security | Sentry client beforeSend does noise-filtering only, no PII scrubbing (defense-in-depth gap) | instrumentation-client.ts:83-103 drops extension noise only; webhook already handles customer email/name nearby; nothing scrubs if PII ever enters an event | No active leak today; latent risk given adjacent PII-handling code paths | S | Add PII-scrub in beforeSend/beforeBreadcrumb; keep sendDefaultPii disabled |

---

## Blocked audit tracks — re-run when access returns

The audit ran under two constraints: (a) a one-live-call-per-area MCP approval gate (only two calls were spent: Semrush domain_organic and GitHub Actions history), and (b) the site + Shopify CDN return 403 to the audit proxy (consistent with the settled bot-block finding). The staged Semrush "mega export" (2026-05-04) turned out to be a site-audit issues export with no keyword/traffic columns and was unusable for organic analysis; the keyword work instead used the fresh 2026-07-03 live pull, which is however limited to the top 500 keywords, one URL per keyword, no history, no SERP features.

When access returns, re-run:

1. **Semrush, URL-level positions export (beyond top-500)** — confirm the "zero rankings" claims for the 26 neighborhood pages, La Brea, and PDPs (currently "absent from top-500", not proven zero); full cannibalization sweep (only same-keyword multi-URL rows in the top 500 were detectable); historical trend + SERP-feature pull.
2. **Live HTTP verification (site + Vercel)** — actual status of /pages/mattress-store-playa-del-rey (seo-organic-04); whether apex serves 200 vs 308 (seo-organic-03, check Vercel domain config); production response headers (HSTS/COOP presence — secpriv-04 assumed from next.config.mjs source only); live 404 status code + post-hydration DOM; live sitemap.xml as served.
3. **Semrush site-audit re-crawl** — the staged crawl predates most shipped fixes (2026-05-04); re-baseline after Batch 2 lands.
4. **Backlinks track** — never ran (no staged data, MCP gated): referring-domain profile for the location pages and the dual-host split's equity impact.
5. **Sentry** — error/perf data was never pulled: 404-route error rates, chat/API failure rates, field CWV if enabled; also verify whether refresh-inventory failures surfaced anywhere.
6. **Vercel** — build output for per-route First Load JS (repo had no completed production build; audit was read-only, so `next build` was not run), /_not-found prerender status, runtime logs for the 404 CSR bailout.
7. **PostHog** — funnels to quantify the 881–1024px tablet share (ux-pdp-01 exposure), PLP CLS tails (ux-plp-06), and replay retention audit after the secpriv-01 fix.
8. **Shopify Admin (live)** — re-pull url-inventory (staged snapshots are 2026-06-18; metafields CSV 2026-05-20, a curated 81-row extract — full metafield coverage, e.g. sleep_positions across all 218 products, was unverifiable); confirm seasonal collection stocking; page bodies for the 27 uncaptured pages.

---

## Recommended fix batches

Batches are one PR each (Batch 4 is a Shopify Admin changeset + one small PR), ordered by value/effort. Together they cover all P1s except secpriv-02 (nonce CSP — a P1/L standalone track that should start as report-only after Batch 3) and the two P1 content items folded into Batch 4.

### Batch 1 — Storefront conversion & trust hotfixes (all S; highest value/effort)
CSS/component one-liners that unblock or de-embarrass the buy flow.
- ux-pdp-01 (tablet sticky-ATC dead zone), ux-css-02 (`--container-pad`), perf-img-01 (raw sticky-bar image), ux-plp-03 (financing math), ux-mobile-04 (compare tray vs ATC bar), ux-mobile-05 (chat FAB vs filter sheet), a11y-contrast-01 (unavailable-chip contrast), perf-img-08 (homepage card preloads).
- **Acceptance:** at 881–1024px, tapping sticky Add-to-cart opens a visible sheet (or the bar is hidden) and the page never scroll-locks without a visible dialog; `.plp-content`/`.plp-sibling-nav`/announcement bar show gutter padding at <1200px; PDP head contains no untransformed image preload; PLP card and PDP quote the identical monthly figure with provider label; compare tray and chat FAB never overlap the ATC bar / 'Show results'; unavailable chips ≥4.5:1; homepage emits exactly one high-priority image preload (the hero).

### Batch 2 — SEO pipeline, titles & indexation (S–M)
Restores the data pipeline and fixes the machine-generated SERP surface, pinned by tests.
- seo-tech-01 (fix + alert the refresh-inventory workflow, land fresh snapshot), seo-tech-02 (BRAND_SUFFIX_RE '·' / ensureTitleDistinctFromH1), seo-organic-03 (apex→www 308 at Vercel/middleware), seo-tech-03 (beds-mattresses sitemap policy + repoint blog automation), codeq-test-gaps-02 (sitemap/seo/middleware tests), codeq-site-const-dup-10 (SITE_URL imports — same drift family as seo-organic-03).
- **Acceptance:** refresh-inventory green with a snapshot commit dated post-fix and failure alerts verified delivered; zero PDP titles containing '· LA Mattres…' (re-run the 218-product simulation = 0 mid-suffix cuts); curl of apex returns 301/308 to www; sitemap and /blogs hub agree on beds-mattresses (either 72 live articles in sitemap or 72 new 301s); new tests fail on regression of each of the above.

### Batch 3 — Analytics privacy & performance diet (S–M)
One coherent pass over the analytics stack: stop the PII leak, cut the tax.
- secpriv-01 (mask replay inputs), perf-3p-02 (sample before init), perf-js-03 (lazy-import posthog-js), perf-3p-05 (gtag lazyOnload), perf-3p-04 (single CWV sink, evaluate dropping @vercel/analytics), codeq-npm-audit-07/secpriv-09 (npm audit fix + CI gate).
- **Acceptance:** replay of a test session shows CCPA/newsletter inputs masked; recorder.js requested only in sampled-in sessions; posthog-js absent from First Load JS on a `next build` diff; no gtag preload in any page head; CWV events arrive in exactly one sink; `npm audit --audit-level=high` = 0 and enforced in CI.

### Batch 4 — Organic quick wins (Shopify changeset + small PR)
Content/redirect work capturing the verified keyword upside; mostly Shopify Admin.
- seo-organic-04 (playa-del-rey redirect — verify live status first), seo-organic-02 (merge the full-size-dimensions cluster, 301 two posts), seo-organic-01 (striking-distance sprint on the 6 target collections — start with helix + mattress-toppers), shopify-hyg-01 (9 collections' seoTitle/seoDescription), shopify-hyg-03 (re-key the 4 sleep_position metafields), seo-organic-08 (La Brea page parity), seo-organic-09 (cal-king crosslink).
- **Acceptance:** /pages/mattress-store-playa-del-rey resolves 301→marina-del-rey (or 200 recreated); one canonical full-size-dimensions URL with two 301s live and the survivors dropped from the sitemap on next refresh; the 6 collections show updated titles/H1/FAQ modules and ≥2 new internal links each; 0 collections with null seoTitle+seoDescription (excluding empty placeholders); the 4 clearance products appear under the Sleep Position filter; re-run Semrush after 4–6 weeks to measure pos 4–10 → top-3 movement.

**Standalone track (not batched):** secpriv-02 nonce-based CSP (P1, L) — start Report-Only alongside Batch 3, drop 'unsafe-inline' once own inline scripts are nonced, isolate Judge.me legacy widget to remove 'unsafe-eval'.

---

## Appendix: killed findings

| id | area | claimed finding | kill reason |
|---|---|---|---|
| secpriv-03 | security | Rate-limit key derived from spoofable leftmost X-Forwarded-For, bypassing all throttles on /api/chat, /api/newsletter, /api/ccpa-request | The cited code is real (lib/rate-limit.ts:91-98 does take the leftmost XFF entry), but the spoofing premise does not hold on this stack: the app deploys on Vercel, whose proxy controls the forwarding chain, so the client cannot freely prepend arbitrary leftmost XFF values as assumed. Residual concern (in-memory per-instance limiter; confirm Vercel's current XFF sanitization) is folded into the secpriv-05/secpriv-06 fixes (trusted-IP key + durable store). |

Also dropped during the code-quality area's own verification (never entered the register): a committed-tsbuildinfo claim (file is gitignored, not committed), an eslint-disable-abuse claim (all 8 sites carry written rationales), and a commented-out-code claim (none found).

---

## Appendix: 404 empty-shell on matched dynamic routes — diagnosed, upstream

**Symptom (seo-tech / issue observed during Batch 6):** unknown handles under a *matched* dynamic route (e.g. `/pages/zzz-unknown-handle`, matched by `app/(storefront)/pages/[handle]`) return HTTP 404 with `<html id="__next_error__">` and an **empty server-rendered body** — the branded not-found content and site chrome arrive only via the RSC flight payload and render client-side. Truly unmatched paths (e.g. `/zzz-nowhere`) serve the fully prerendered branded `/404` correctly.

**What was ruled out in our code:**
- PR #504 moved the `cookies()` (preview-mode) read *after* the `notFound()` gates in both `generateMetadata` and the page component — correct hygiene, but the shell persisted.
- Retested 2026-07-04 on **next 15.5.20** (PR #507; production deployment `bef91cd` confirmed READY before the fetch): `x-nextjs-prerender: 1`, status 404, correct `<title>Page not found · LA Mattress Store</title>` and meta in `<head>`, visible body text = 0 chars, `__next_error__` shell still present.

**Conclusion:** this is framework behavior for `notFound()` thrown from a matched dynamic route in this App Router setup (Next 15.5.x on Vercel), not an app bug we can patch — the route-level not-found boundary streams the error shell and hydrates the branded content client-side.

**Impact assessment (why this is acceptable to park):** status code (404), title, and robots meta are all correct — crawlers see the right signals; Google renders JS anyway; the page is a dead-end 404 by design. The only real cost is JS-disabled users seeing a blank 404 body.

**Re-check trigger:** on future Next.js major/minor upgrades, re-fetch `/pages/zzz-unknown-handle` and check for `__next_error__` + empty body. If Next ships a fix, no app change should be needed.

---

## Appendix: 2026-07-05 crawl triage (round 4)

Snapshot `6a49a336b8060ebdf5c70d9c` (03:29 UTC, first crawl covering all 9 fix batches). **Quality 95 (+1), errors 5 (−26), warnings −40.** Wins confirmed: issue 105 h1/title duplicates **46 → 0** (PDP `ensureTitleDistinctFromH1` guard), 4xx pages **9 → 1**, broken internal links **22 → 4**.

**Fixed this round:**
- The remaining 4xx and all 4 broken internal links were one URL: `/pages/quiz` (linked from 4 buying-guide article bodies; the quiz lives at `/sleep-quiz`). Shopify urlRedirect `gid://shopify/UrlRedirect/489990881533` created + mirrored in `redirects-manual.json` → middleware 301. Issues 2 and 8 should read 0 next crawl (the 4 links migrate to notice 214 until the article bodies are edited — needs `graphql_query` access, still approval-gated this session).

**Checked and closed as stale crawl data (no change made — verified live 2026-07-05):**
- Issue 214 rows claiming `/pages/mattress-sizes` links 301 (`king-vs-california-king`, `full-size-bed-dimensions-width-couples`, `queen-mattress-size-guide-…`): all three URLs return 200 with self-canonicals today. Transient article-handle churn, since reverted. `lib/mattress-sizes-data.ts` left untouched — "fixing" to Semrush's targets would have pointed at the wrong URLs.
- Issue 214 row claiming the locations hub links a redirecting `marina-del-rey`: live, marina IS the page (200, neighborhood template) and the manual layer already 301s playa→marina. No change.

**Round 5 (2026-07-06): article-body fixes via the cleanup pipeline.** The four quiz-linking articles were published Jul 4–5 by the **external daily-blog automation** (absent from the Jul 3 inventory pull) — its template writes `/pages/quiz`, so new articles will keep recreating the broken link until the merchant fixes that template. Rather than one-off body edits, the fix went into `scripts/seo-article-cleanup.mjs`: a new `quiz_href_rewrites` pass (relative + absolute forms, idempotent) and a `semrush-2026-07-05-quiz-and-ext` batch covering the 4 quiz articles plus the 4 broken-external-link articles (pass 4 unwraps their external anchors — same posture as the two prior broken-ext batches). Run via Actions → "SEO — blog article cleanup" with that batch + apply=true; SHA-gated with auto-rollback.

**Documented, no action:**
- Issue 12 external-link detail: 3× researchgate.net respond 429 to crawlers (bot rate-limiting; fine for humans) and 1× iblspecifik.com 503 (genuinely unstable) — all four articles included in the round-5 batch above; pass 4 unwraps the anchors, keeping the visible text.

**Round 6 (2026-07-07 crawl, snapshot `6a4c45f9708a1073bbac99c0`):** quality 97 (−1). Two changes triaged:
- **Issue 12 grew 4 → 11:** external-domain decay on old articles, not new content — healthline.com reorganized its `/health/*` URLs (5 anchors now 404) and us-mattress.com now answers crawlers 423 (3 anchors). Remedy: new **superset batch `semrush-2026-07-07-all-open`** (15 articles = the unapplied 07-05 cohort + the 7 new) so a single apply run clears the whole backlog once the token is fixed. The cleanup pipeline is still blocked — `SHOPIFY_ADMIN_TOKEN` failed 401 again on the Jul 6 and Jul 7 scheduled inventory runs.
- **Issue 111 (1 too-slow page, 5.2s):** the just-published `best-mattress-features-for-aging-bodies…` article's first-ever render (ISR cold start — the crawler was its first visitor). Verified live at 200/fast; transient, self-heals in the next crawl. Errors otherwise remain 0; issues 2/8/105 all hold at 0.
- Issue 206 (206 GA orphans, +7): the historical GA tail — old blog URLs, `.json` product endpoints, retired sale handles that still log GA sessions while 301ing. Redirects are already in place for everything fixable; the count decays with traffic.
- Issues 213 (+3), 112 (1225), 223 (89), 4 (276 robots-blocked): unchanged posture — fluctuating notices / content-strategy items / intentional blocks.

---

## Appendix: design notes for the two deferred L items

Written 2026-07-04 after Batches 7–9 shipped. Both items are implementable but need a decision/measurement first — captured here so the next round starts from the analysis, not from scratch.

> **Status update (2026-07-11, round 9):** option 3 below is **implemented** — `lib/plp-cache.ts` + a middleware step set `Cache-Control: public, s-maxage=300, stale-while-revalidate=600` on canonical param-less `/collections/[handle]` responses. The measurement gate was satisfied by crawler evidence instead of PostHog (the PostHog MCP tool is approval-gated): the 07-07 and 07-11 crawls each caught a ~5s first-visitor render (issue 111 — an article cold start, then `/collections/tempur-pedic-adjustable-bases`), pinning the performance score at 95. Related: the *article* flavor of those cold starts heals once `SHOPIFY_ADMIN_TOKEN` is fixed — the daily inventory refresh redeploys, prerendering new articles before the crawler arrives. Round-9 triage also confirmed the GA-orphan growth (220 → 247) is sale-week traffic on already-redirected legacy URLs — decays, no action.

### perf-isr-07 — PLP ISR

**Current state:** `app/(storefront)/collections/[handle]/page.tsx` sets `export const dynamic = 'force-dynamic'` because sort/pagination/filter state lives in `searchParams`. Every PLP request — including the param-less canonical view that crawlers and most organic landings hit — is server-rendered per request. The *data* layer is already cached (Shopify fetches carry `revalidate` windows), so the per-request cost is render time + function cold starts, not API round-trips.

**Options considered:**
1. **PPR (Partial Prerendering)** — the "right" long-term answer (static shell + dynamic holes for the filter/sort-dependent grid), but still experimental on Next 15.5.x; not appropriate for this production store yet.
2. **Route split** (static `/collections/[handle]` + separate dynamic browse route for param'd views) — changes URLs for filtered/sorted views, which are already canonicalized and crawl-managed; SEO churn outweighs the render savings. Rejected.
3. **CDN caching for the param-less view (recommended first step):** dynamic render stays, but responses without query params get `Cache-Control: s-maxage=300, stale-while-revalidate=600` so Vercel's CDN serves repeat hits without invoking the function. Small, reversible, and targets exactly the request class that dominates PLP traffic. Param'd views (sort/filter/pagination) remain uncached per request.

**Before implementing:** pull p75 TTFB per PLP route from PostHog `web_vital` events to size the win — if p75 TTFB on canonical PLP views is already <300ms, option 3's user-visible gain is marginal and the item can be closed as "not worth the cache-invalidation complexity". Revisit PPR when it reaches stable.

### ux-css-08 — breakpoint consolidation

**Census (globals.css, post-Batch-8):** `max-width: 760px` ×25, `880px` ×16, `640px` ×14, `720px` ×13, `1024px` ×12, `480px` ×10, `768px` ×6, `600px` ×7, plus a long tail (520, 560, 420, 900, 980, 1200). The sticky-bar/sheet cluster was already aligned at 1024px in Batch 1.

**Proposed canonical scale:** 480 (small phone) / 640 (large phone) / 768 (tablet portrait) / 1024 (desktop) / 1200 (wide) — matching the industry-default scale so future contributors guess right.

**First slice:** merge the 720/760/768 "tablet" cluster onto 768. The risk window is real devices in the 721–768px band — most notably iPad Mini portrait at exactly 768px, which would flip into mobile treatment for every migrated `max-width: 760px` rule. That's usually the *correct* behavior (those rules exist to linearize multi-column layouts that cramp at tablet width), but it must be eyeballed, not sed'd: migrate template-by-template (PLP first, then PDP, then CMS chrome) with a device-width spot check per template. Not started this round because each slice needs visual review the CI suite can't provide.

---

## Verification note

Top-P1 evidence was independently re-checked in the main session before publication (2026-07-02): ux-pdp-01 (globals.css:2845/2891 + buy-box.tsx:345 — dead zone is 881–1100px, wider than the auditor's 881–1024px), ux-css-02 (5 usages, 0 definitions), secpriv-01 (maskAllInputs:false, zero data-ph-mask usages), seo-tech-02 (BRAND_SUFFIX_RE missing '·'; live PDP title renders "· LA Mattres…"), perf-img-01 (raw untransformed preload confirmed in capture head), seo-tech-01 (last inventory snapshot commit cbc8d18, 2026-06-18). All confirmed.
